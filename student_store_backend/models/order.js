const db = require("../db")

class Order {
    static async listOrdersForUser() {
        // return all orders that the user has created
    }

    static async createOrder() {
        // take a users order and store it in the database
    }
}

module.exports = Order